#! /usr/bin/python3

import json

taille = 255

class Noeud():
	
	def __init__(self):
		
		self.address
		self.cle
		self.data
		self.precedent
		self.suivant
	
	def join(key,value):
		
		if key < 0 or key > taille :
			print("la cle doit etre comprise entre 0 et"+taille)
		else:
			self.cle = cle
			self.val = value
			self.precedent = self.setprecendent
			self.suivant = self.setsuivant
			
	    
	    def setsuivant(self, successor):
			self.suivant = successor
		
		def setprecedent(self,pred):
			self.precendent =pred
			
			
		def addToCircle():
			print("a")
		
		
		

